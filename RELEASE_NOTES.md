# InkForge v1.2.0 版本说明

> 本版本聚焦**版权合规**：下线了依赖第三方样式中心的样式库集成（对应的服务端路由、客户端面板与插入组件，以及未入库的本地素材目录均已移除），并把界面与外部素材相关的「抓取」表述统一为「导入 / 提取」。

## 基本信息

| 项目 | 内容 |
|---|---|
| 版本号 | **1.2.0** |
| 许可协议 | MIT |
| 运行环境 | 内置 Node.js（解压即跑），无需另行安装 |
| 默认端口 | API / 生产前端 `5177` |
| 数据库 | better-sqlite3（本地文件，无需额外服务） |

## 本版本变更

- **移除第三方样式库接入**：左侧「样式库」面板、编辑器内的样式插入组件，以及服务端的样式检索路由全部下线；交互组件、文档模板、片段、素材库等原有能力不受任何影响。
- **清理外部素材表述**：外链图片入库、从文章链接提取生态组件等通用导入能力保留，但界面与提示文案中的「抓取」统一改为「导入 / 提取」，消除歧义。
- **代码合规**：全仓提交信息与源码中不再出现第三方品牌名与「抓取」字样。

## 与历史版本关系

- v1.0.0 / v1.1.0 作为历史快照保留，本版本为在两者基础上的合规化重构，**不改动既有快照**。

## 这个版本能做什么

- 可视化编辑微信公众号图文：区块、组件、卡片、时间线、步骤、手风琴、SVG、Lottie、视频、音频、二维码、交互组件等。
- Markdown 双向互转、浮动图文混排、精细字号与排版控制。
- 微信生态组件（小程序 / 视频号）提取、外链图片导入到本地素材库。
- 片段与模板库、历史版本、账号管理与一键群发草稿。
- 导出为微信兼容 HTML（自带清洗白名单，符合平台约束）。
- 内置运行时，解压即跑，数据本地持久化。
